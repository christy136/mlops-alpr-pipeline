apiVersion: v1
kind: Service
metadata:
  name: alpr-service
spec:
  type: NodePort
  selector:
    app: alpr-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007  # Port to access the service externally

